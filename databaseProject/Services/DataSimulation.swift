//
//  DataSimulation.swift
//  databaseProject
//
//  Created by Noah Iarrobino on 4/7/20.
//  Copyright © 2020 Noah Iarrobino. All rights reserved.
//

import Foundation

class DataSimulation {
    
    static let instance = DataSimulation()
    /*
    func createData() {
        let add = Exercise(name: "Bench")
        WorkoutService.instance.exerciseList.append(add)
        let one = Modifier(name: "belt", modType: "kit")
        let two = Modifier(name: "knee wrap", modType: "kit")
        WorkoutService.instance.exerciseList[0].modifiers.append(one)
        WorkoutService.instance.exerciseList[0].modifiers.append(two)

        //bar kit rom tempo
        let a = Modifier(name: "swiss bar", modType: "bar")
        let b = Modifier(name: "1 board", modType: "ROM")
        let c = Modifier(name: "tempo iso", modType: "tempo")
        WorkoutService.instance.exerciseList[0].modifiers.append(a)
        WorkoutService.instance.exerciseList[0].modifiers.append(b)
        WorkoutService.instance.exerciseList[0].modifiers.append(c)

        
        
        
        
    }
 */
}
